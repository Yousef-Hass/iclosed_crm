export enum ViewType {
  KANBAN = 'KANBAN',
  TABLE = 'TABLE'
}

export enum FieldType {
  CONTACT = 'CONTACT',
  EVENT = 'EVENT',
  PIPELINE = 'PIPELINE'
}

export enum UserRole {
  ADMIN = 'ADMIN',
  USER = 'USER'
}
